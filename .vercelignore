# Frontend needs src/ (engine) + web/ + server/src (type-only protocol import).
# Skip the rest so uploads stay small. server/node_modules and *.db are dropped
# by the node_modules / *.db rules below.
e2e
test
node_modules
web/node_modules
*.pdf
*.png
*.gif
*.webp
*.db
*.db-shm
*.db-wal
laska*.html
lasca*.html
inspiration.html
design-3d-test

# …but the served web/public/ assets must reach the build (vite copies them into
# web/dist). Re-include them so the *.pdf / *.png globs above don't strip them.
!web/public/laska-rules-1911.pdf
!web/public/young-emanuel-lasker.png
